import React from 'react';

/**
 * TODO: Implement Friends Feature
 * 
 * Future Implementation Details:
 * - Implement friend requests system (add, accept, reject)
 * - Fetch pending friend requests from /api/friend/request/pending
 * - Fetch accepted friends from /api/friend/getfriends
 * - Handle removing friends via /api/friend/remove
 */
const Friends = () => {
    return (
        <div className="empty-message">
            <h2>Friends</h2>
            <p>This feature is currently under development.</p>
        </div>
    );
};

export default Friends;
